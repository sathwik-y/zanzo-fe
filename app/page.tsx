import { Landing } from "@/components/landing/landing";

// The landing page lives at / for everyone — signed-in users get here via the
// logo and head back to the app at /home.
export default function LandingPage() {
  return <Landing />;
}
